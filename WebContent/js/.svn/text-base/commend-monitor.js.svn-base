var CommendMonitor = function() {
    var self = this;

    (function() {
    })(jQuery);

    self.load = function() {
    };
    self.save = function() {
    };
    
    return self;
    
    function loadSympFuncScalePopup() {
        /*
        $('#phqTextId').ForceNumericOnly();
        self.temporaryPatientData = self.patientData.symFunScale.getClone();
        if(self.temporaryPatientData.PHQ!=null) {
            $('#phqTextId').val(self.temporaryPatientData.PHQ);
        }
        */
    }
};